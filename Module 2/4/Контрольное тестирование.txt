Как расшифровывается аббревиатура PHP?
Hypertest PreProcessor

Когда был создан PHP?
1994

Кем был создан PHP (автор)?
Расмус Лердорф

Какие теги используются для перехода к коду PHP?
<?php ?>

Каждое PHP выражение должно заканчиваться…
Точкой с запятой

Выберите префикс, который необходим для создания переменных в PHP
$

Какая из этих переменных не является валидной в PHP?
$my-variable

Как присвоить значение переменной?
$num = 4;

Как писать многострочный комментарии в PHP?
/* это комментарий */

Как определить константу в PHP?
difine("FOO", "BAR")

то будет выведено после выполнения следующего кода?
<div>
4 + 2 = <?= 4+2 ?>
</div>
4 + 2 = 6

Какой тип данных будет у переменной $p после выполнения кода $p = '3.14'?
string

Какое значение будет в переменной $result после выполнения кода $result = 4 % 2;?
0

Какое значение будет в переменной $result после выполнения кода $result = 2 ** 3;?
8

Почему после выполнения кода $a = (8 === "8"); в переменной $a значение false?
Разные типы

Почему после выполнения кода $a = (8 != "8"); в переменной $a значение false?
Значения равны

Какая из этих конструкций не относится к циклам?
isset

Какое значение примет переменная $a после выполнения кода $a=12; $a .= 5;?
125

Где правильно объявлен массив?
$arr = [];

Где неправильно объявлена функция?
Все верно

Как получить данные POST-запроса?
Через переменную $_POST

Что произойдет после выражения include info.php, если файлa info.php не существует?
Продоолжится выпролнение с осообщением об ошибке

Какое значение примет переменная $a после выполнения кода $a= require info.php;, если в
info.php следующий код?
<?php return 7%4;?>
3

Есть массив $arr. Как вывести все элементы массива $arr?
print_r($arr);

Есть массив: $arr = [1, 2, 3, 4, 5, 6]. Как из этого массива получить элемент 5?
$str[4];

